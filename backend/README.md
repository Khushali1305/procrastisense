# Backend (Planned)

Not yet started. Planned for final submission: a lightweight API layer
(likely FastAPI) wrapping the trained models in `models/` so the frontend
can request a live nudge for a given student without re-running the
full notebook.
